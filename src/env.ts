import { defineEnvVars } from '@sveltejs/kit/env';

// @migration-task Review usage of dynamic environment variables. They fall back to the empty string if not present, which may not be what you want.
export const variables = defineEnvVars({
	GEMINI_API_KEY: { schema: (input) => input ?? '' },
	GOOGLE_CLIENT_ID: { schema: (input) => input ?? '' },
	GOOGLE_CLIENT_SECRET: { schema: (input) => input ?? '' },
	GITHUB_CLIENT_ID: { schema: (input) => input ?? '' },
	GITHUB_CLIENT_SECRET: { schema: (input) => input ?? '' },
	GITLAB_CLIENT_ID: { schema: (input) => input ?? '' },
	GITLAB_CLIENT_SECRET: { schema: (input) => input ?? '' },
	BETTER_AUTH_URL: { schema: (input) => input ?? '' },
	PUBLIC_APP_NAME: { schema: (input) => input ?? '' },
	PUBLIC_APP_URL: { schema: (input) => input ?? '' },
	BETTER_AUTH_SECRET: { schema: (input) => input ?? '' },
	EMAIL_PROVIDER: { schema: (input) => input ?? '' },
	SMTP_HOST: { schema: (input) => input ?? '' },
	SMTP_PORT: { schema: (input) => input ?? '' },
	SMTP_USER: { schema: (input) => input ?? '' },
	SMTP_PASSWORD: { schema: (input) => input ?? '' },
	SMTP_FROM: { schema: (input) => input ?? '' },
	RESEND_API_KEY: { schema: (input) => input ?? '' },
	STORAGE_PROVIDER: { schema: (input) => input ?? '' },
	LOCAL_STORAGE_PATH: { schema: (input) => input ?? '' },
	PUBLIC_STORAGE_URL: { schema: (input) => input ?? '' },
	R2_ACCOUNT_ID: { schema: (input) => input ?? '' },
	R2_ACCESS_KEY_ID: { schema: (input) => input ?? '' },
	R2_SECRET_ACCESS_KEY: { schema: (input) => input ?? '' },
	R2_BUCKET_NAME: { schema: (input) => input ?? '' },
	R2_PUBLIC_URL: { schema: (input) => input ?? '' },
	S3_REGION: { schema: (input) => input ?? '' },
	S3_ACCESS_KEY_ID: { schema: (input) => input ?? '' },
	S3_SECRET_ACCESS_KEY: { schema: (input) => input ?? '' },
	S3_BUCKET_NAME: { schema: (input) => input ?? '' },
	S3_PUBLIC_URL: { schema: (input) => input ?? '' },
	MAX_VIDEO_SIZE_MB: { schema: (input) => input ?? '' },
	ENABLE_SERVER_COMPRESSION: { schema: (input) => input ?? '' },
	VIDEO_THUMBNAIL_ENABLED: { schema: (input) => input ?? '' },
	STRIPE_SECRET_KEY: { schema: (input) => input ?? '' },
	STRIPE_WEBHOOK_SECRET: { schema: (input) => input ?? '' },
	STRIPE_PRO_PRICE_ID: { schema: (input) => input ?? '' },
	STRIPE_TEAM_PRICE_ID: { schema: (input) => input ?? '' },
	PAYSTACK_SECRET_KEY: { schema: (input) => input ?? '' },
	PAYSTACK_PRO_PLAN_CODE: { schema: (input) => input ?? '' },
	PAYSTACK_TEAM_PLAN_CODE: { schema: (input) => input ?? '' },
	RATE_LIMIT_ENABLED: { schema: (input) => input ?? '' },
	RATE_LIMIT_MAX_REQUESTS: { schema: (input) => input ?? '' },
	RATE_LIMIT_WINDOW_MS: { schema: (input) => input ?? '' },
	DATABASE_URL: { schema: (input) => input ?? '' }
});
